date01
	2章自读，三章开始
	
	3.1~3.12 3.14其他自读



三那个文件大小查看的命令
ls -l xxx
stat xxx
od -c xxx

小心空洞文件下载或复制的化文件会很大。
time ./xxx.run

作业3.4 和3.5

2>&1    表示2是由1复制的2>&1    表示2是由1复制的

open函数的第二个参数中的O_APPEND 的这个宏十分的霸道，只要用这个后，lseek就不顶用了，即使是蛇者了pos直，当写操作时仍然是按照元文件末端追加

date02
	5.1

date03
	4.1
	实现 ls -l -a -i -h

	堆或静态区 /r?

	$算法代号（6--shd512）$颜直$
	strftime(4)

date04
	getopt	


date05
	第二部分：7 8 9 10 13 15
	

1.进程终止方式背过。
2.要背过的图
	3.10 图 文件2个 
	6.10 时间一个 
	7.6  c程序存储 
	7.3  进程启动和终止

3.echo $? 获取上一个进程的终止状态
问题1：_exit()只是关闭文件符，静态区和堆呢?

进程结束后内核会关闭文件符，free静态区，堆等所有的空间都会由内核处理掉
即使已经并非关闭的文件符，再通过内核处理也不会报错。答案同样可以解决一
问题2。


问题2:exit（）会处理缓存区，会不会关闭文件服，free静态区和堆，如果是
	使用atexit（）会不会报错。

4.环境变量/表?

jsw $ export PATH+=:./

资源获取函数grlimit

下周话题GDB/git

1 init进程 是用户态的第一个进程

ps axj 打印所有进程信息

一般如果父进程比子进程先结束，会被其他进程接管。
fork:
	缓存区会被复制，如果不想被复制用fflush(NULL)刷新一下
	只会复制进程表项，不能复制文件表项
	写时复制——原本fork复制的也表与父进程一样，但是当要该表其对应内存中
			  的数据时，其虚拟内存与物理内存对应的也表信息要改变。

作业
	模拟shell外部命令
	并发筛选质数
		100-300之案件的所有指数输出，一个进程筛选出一个。


date06
	-20~19 优先级高到低
	有效id 用户id 保存设置用户id
	进程会计acct(3) getlogin(3) 
	进程时间

	13章守护
	子进程会继承父进程的组id 绘画id,故子进程一定不是一个leader除非用se-
	tsid()

	/dev/null

	守护进程建议——
					文件0 1 2重定向
					文件的权限的屏蔽字为0
					执行路径该为/
	标准输入输出错误是行缓存，其他文件不是。
	
	找日志文件：
	find / var -name "syslog"各个操作系统不一样
	进程结束时文件锁会释放。


date07
	存储核心文件的现场内容。
	jsw@~$ ulimit
	
	标准信号是位图记录，会丢失
	实时信号是队列记录，不会丢失

	信号会打断阻塞的系统调用

	信号处理函数不能用setjump要用siglongjump 因为mask要等嗲被待用handler
	返回时重置
	信号的嵌套—— 一个信号处理函数没有完时又来了另一个不同的信号
	gcc 
		-O2 //优化级别2,选项O代表优化级别。

	漏桶
		轮询
		通知
	信号

date08 
	作业:实现myalarm(不覆盖)
	自己指定行为函数，自己传信号的地址。

	有符号数和无符号数强转
		运算
		有符号转成无符号，但是注意格式化字符输出时又会发生一次强转

		低精度变量与高精度变量运算会向高精度转换。

		所有系统调用部分库可以重入调用。

date09
	mplayer更多说明看 man mplayer或者用选项-cache
	$mkfifo “”//创建管道文件
	fifo和pipe区别是fifo是一个真是的文件，同一时刻只能写或读，不是亲缘
	进程也能通信。

	作业：统计文件中字符串个数

date10
	查看线程命令
	$ ps axm -L

	main return 与其他线程的return不一样

date11
	
	非阻塞的io不会被信号打断，但是有一种假错误是，使用非阻塞方式打开却发生了
	阻塞（套接字文件）。	
	
	ctrl + fn +alt f9 一个自带字符设备
	ctrl + fn +alt f7 恢复

	vim多行替换
	:行号1,行号2 s/str1/str2/g

date12
	用多线程实现流量控制
	1024~65534是用户端口
	0~1024是公用的
	A类型网络 网络号0开始
	0.0.0.0代表任意地址
	x.x.x.x最后一个x的255是广播地址。
	B类型网络 网络号10开始
	c类型网络 网络号110开始
	D类型网络 网络号1110开始

date13
	$ netstat -aun	//查看所有报式网络
			  -atp	//查看所有流式网络

	$ sudo ufw disable //关闭防火墙

	$ sudo wireshark //抓网络包工具
		ip.addr == 172.25.1.173&&ip.dst == 255.255.255.255
		udp.port eq 3333

date14
	sigaction()的SA_NOCLDWAIT
	不等带子进程

date15

